# -*- coding: utf-8 -*-
"""Logistic_Regression.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/19Bvxu57sPXeDCh-w6m_jSOdKVsqatSgi

#**Heart Attack Risk Classification**

**problem statement**
The goal of this project is to develop a classification model that can predict the likelihood of a heart attack based on various patient health indicators. The dataset contains information on patients' demographic details, medical history, and diagnostic measures. The primary objective is to accurately classify patients into two categories: those with a higher likelihood of experiencing a heart attack (target = 1) and those with a lower likelihood (target = 0).

**Import Libraries**
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

"""**Load Data**"""

data=pd.read_csv('heart.csv')
data

"""##**Understanding Variables**
Age – Age of the patient in years.

Sex – Gender of the patient (M = Male, F = Female).

ChestPainType – Type of chest pain experienced by the patient.

RestingBP – Blood pressure level when the patient is at rest.

Cholesterol – Cholesterol level in the patient’s blood.

FastingBS – Whether fasting blood sugar is high (1 = Yes, 0 = No).

RestingECG – Result of the resting electrocardiogram test.

MaxHR – Maximum heart rate achieved during exercise.

ExerciseAngina – Whether chest pain occurs during exercise (Y = Yes, N = No).

Oldpeak – ST depression value measured during exercise (heart stress indicator).

ST_Slope – Slope pattern of the ST segment during peak exercise.

HeartDisease – Whether the patient has heart disease (1 = Yes, 0 = No).

**Basic check**
"""

data.shape

data.head()

data.tail()

data.info()

data.describe()

cat_col=data.select_dtypes(include=['object'])
cat_col

num_col=data.select_dtypes(include=['object'])
num_col

for x in cat_col:
  print(x)
  print(data[x].value_counts())
  print()

data.dtypes

data.columns

data.describe(include='object')

"""##**Data Cleaning**

**Check Missing Values**
"""

data.isnull().sum()

data.duplicated().sum()

"""**Exploratory data analysis**

**Univariate**
"""

#check distribution of age with heart disease
plt.figure(figsize=(6,6))
sns.histplot(data,x='Age',kde=True)
plt.show()

#check distribution of RestingBP with heart disease
plt.figure(figsize=(5,3))
sns.histplot(data,x='RestingBP',kde=True)
plt.show()

#check distribution of cholosterol with heart disease
plt.figure(figsize=(5,2))
sns.histplot(data,x='Cholesterol',kde=True)
plt.show()

#check distribution of Max HR with heart disease
plt.figure(figsize=(5,2))
sns.histplot(data,x='MaxHR',kde=True)
plt.show()

#Countplot for sex
plt.figure(figsize=(5,2))
sns.countplot(data,x='Sex',hue='HeartDisease')
plt.show()

#Countplot for chestpain
plt.figure(figsize=(5,2))
sns.countplot(data,x='ChestPainType',hue='HeartDisease')
plt.show()

#Countplot for RestingECG
plt.figure(figsize=(5,2))
sns.countplot(data,x='RestingECG',hue='HeartDisease')
plt.show()

#Countplot for ExerciseAngina
plt.figure(figsize=(5,2))
sns.countplot(data,x='ExerciseAngina',hue='HeartDisease')
plt.show()

#Countplot for ST_Slope
plt.figure(figsize=(5,2))
sns.countplot(data,x='ST_Slope',hue='HeartDisease')
plt.show()

#check outliers of age
plt.figure(figsize=(5,2))
sns.boxplot(data,x='Age')
plt.show()

#check outliers of restingBP
plt.figure(figsize=(5,2))
sns.boxplot(data,x='RestingBP')
plt.show()

#check outliers of cholestrol
plt.figure(figsize=(5,2))
sns.boxplot(data,x='Cholesterol')
plt.show()

#check outliers of MaxHR
plt.figure(figsize=(5,2))
sns.boxplot(data,x='MaxHR')
plt.show()

#check outliers of oldpeak
plt.figure(figsize=(5,2))
sns.boxplot(data,x='Oldpeak')
plt.show()

"""**Bivariate**"""

# Check age vs RestingBP
plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Age',y='RestingBP',hue='HeartDisease')

# Check age vs Cholestrol
plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Age',y='Cholesterol',hue='HeartDisease')

# Check age vs MaxHR
plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Age',y='MaxHR',hue='HeartDisease')

# Check age vs OldPeak
plt.figure(figsize=(5,2))
sns.scatterplot(data,x='Age',y='Oldpeak',hue='HeartDisease')

#barplot for chestpain v/s cholestrol
plt.figure(figsize=(5,2))
sns.barplot(data,x='ChestPainType',y='Cholesterol')
plt.show()

#barplot for chestpain v/s restingBP
plt.figure(figsize=(5,2))
sns.barplot(data,x='ChestPainType',y='RestingBP')
plt.show()

#barplot for ST_Slope vs cholestrol
plt.figure(figsize=(5,2))
sns.barplot(data,x='ST_Slope',y='Cholesterol')
plt.show()

#barplot for ST-Slope vs RestingBP
plt.figure(figsize=(5,2))
sns.barplot(data,x='ST_Slope',y='RestingBP')
plt.show()

"""##**multivariate Analysis**"""

sns.pairplot(data,hue='HeartDisease')

"""##**Hanling Outliers**"""

fig,ax=plt.subplots(1,5,figsize=(6,2))
sns.boxplot(data,x='Age',ax=ax[0])
sns.boxplot(data,x='RestingBP',ax=ax[1])
sns.boxplot(data,x='Cholesterol',ax=ax[2])
sns.boxplot(data,x='MaxHR',ax=ax[3])
sns.boxplot(data,x='Oldpeak',ax=ax[4])
plt.tight_layout()

fig,ax=plt.subplots(1,5,figsize=(6,2))
sns.histplot(data,x='Age',ax=ax[0])
sns.histplot(data,x='RestingBP',ax=ax[1])
sns.histplot(data,x='Cholesterol',ax=ax[2])
sns.histplot(data,x='MaxHR',ax=ax[3])
sns.histplot(data,x='Oldpeak',ax=ax[4])
plt.tight_layout()

df=['RestingBP','Cholesterol','MaxHR','Oldpeak']
for x in df:
    print(x,'-->',data[x].skew())
    print()

#RestingBP,MaxHR are normal
#Cholestrol,Oldpeak are skewed

"""**Cholestrol**"""

#IQR Method:Cholestrol distribution is skewed
Q1=data.loc[:,'Cholesterol'].quantile(0.25)
Q3=data.loc[:,'Cholesterol'].quantile(0.75)
IQR=Q3-Q1
lower_limit=Q1-1.5*IQR
upper_limit=Q3+1.5*IQR

#filter all the values greater than upper and less than lower
data[(data.loc[:,'Cholesterol']>upper_limit)|(data.loc[:,'Cholesterol']<lower_limit)]

len(data[(data.loc[:,'Cholesterol']>upper_limit)|(data.loc[:,'Cholesterol']<lower_limit)])/len(data)*100

"""Since % of outliers is more than 5%will leave them as it is

**oldpeak**
"""

#IQR Method:Cholestrol distribution is skewed
Q1=data.loc[:,'Oldpeak'].quantile(0.25)
Q3=data.loc[:,'Oldpeak'].quantile(0.75)
IQR=Q3-Q1
lower_limit=Q1-1.5*IQR
upper_limit=Q3+1.5*IQR

data[(data.loc[:,'Oldpeak']>upper_limit)|(data.loc[:,'Oldpeak']<lower_limit)]

len(data[(data.loc[:,'Oldpeak']>upper_limit)|(data.loc[:,'Oldpeak']<lower_limit)])/len(data)*100

"""since % of outliers is less than 5% lets replace with median(distribution is skewed)"""

data.loc[(data.loc[:,'Oldpeak']>upper_limit)|(data.loc[:,'Oldpeak']<lower_limit),'Oldpeak']=data.loc[:,'Oldpeak'].median()

len(data.loc[(data.loc[:,'Oldpeak']>upper_limit)|(data.loc[:,'Oldpeak']<lower_limit)])/len(data)*100

"""**RestingBP**"""

mean_value=data['RestingBP'].mean()
std_value=data['RestingBP'].std()
upper_limit=mean_value+(3*std_value)
lower_limit=mean_value-(3*std_value)

data[(data.loc[:,'RestingBP']>upper_limit)|(data.loc[:,'RestingBP']<lower_limit)]

len(data[(data.loc[:,'RestingBP']>upper_limit)|(data.loc[:,'RestingBP']<lower_limit)])/len(data)*100

data.loc[(data.loc[:,'RestingBP']>upper_limit)|(data.loc[:,'RestingBP']<lower_limit),'RestingBP']=data.loc[:,'RestingBP'].mean()

"""#**MaxHR**"""

mean_value=data['MaxHR'].mean()
std_value=data['MaxHR'].std()
upper_limit=mean_value+(3*std_value)
lower_limit=mean_value-(3*std_value)

data[(data.loc[:,'MaxHR']>upper_limit)|(data.loc[:,'MaxHR']<lower_limit)]

data.loc[(data.loc[:,'MaxHR']>upper_limit)|(data.loc[:,'MaxHR']<lower_limit),'MaxHR']=data.loc[:,'MaxHR'].mean()

"""#Encoding"""

#sex,ExerciseAngina,ST_Slope,RestingECG,	ChestPainType

#ExerciseAngina
from sklearn.preprocessing import LabelEncoder
scaler=LabelEncoder()
data.loc[:,'Exercise_Angina']=scaler.fit_transform(data.loc[:,'ExerciseAngina'])

#sex
data=pd.get_dummies(data,columns=['Sex'],dtype='int')

#ChestPainType
plt.figure(figsize=(5,2))
sns.countplot(data,x='ChestPainType',hue='HeartDisease')
plt.show()

#use map
data.loc[:,'Chest_PainType']=data.loc[:,'ChestPainType'].map({'ASY':3,'NAP':2,'ATA':1,'TA':0})

#RestingECG
plt.figure(figsize=(5,2))
sns.countplot(data,x='RestingECG',hue='HeartDisease')
plt.show()

#RestingECG
data.loc[:,'Resting_ECG']=data.loc[:,'RestingECG'].map({'Normal':0,'ST':1,'LVH':2})

#ST_Slope
plt.figure(figsize=(5,2))
sns.countplot(data,x='ST_Slope',hue='HeartDisease')
plt.show()

data.loc[:,'st_Slope']=data.loc[:,'ST_Slope'].map({'Up':1,'Flat':2,'Down':0})

data.drop(columns=['ExerciseAngina','ChestPainType','RestingECG','ST_Slope'],inplace=True)

data

"""##**scaling**"""

#standard scaler
#age,restingBP,cholesrtrol MaxHR
from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
data[['Age']]=scaler.fit_transform(data[['Age']])

from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
data[['RestingBP']]=scaler.fit_transform(data[['RestingBP']])

from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
data[['Cholesterol']]=scaler.fit_transform(data[['Cholesterol']])

from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
data[['MaxHR']]=scaler.fit_transform(data[['MaxHR']])

"""#Feature Engineering"""

#nothing to drop

"""#split data for  x and y"""

x=data.drop(columns=['HeartDisease'])
y=data.loc[:,'HeartDisease']

"""#split data for training and testing"""

from sklearn.model_selection import train_test_split

x_train, x_test, y_train,y_test = train_test_split(x, y, test_size=0.2,random_state=42)

"""##**Balancing Data**"""

plt.figure(figsize=(5,2))
sns.countplot(data,x='HeartDisease')
plt.show()

from imblearn.over_sampling import SMOTE
from collections import Counter
smote=SMOTE(random_state=42)
x_sampled,y_sampled=smote.fit_resample(x_train,y_train)
#balancing done only on training data

"""##**Logistic Regression**"""

from sklearn.linear_model import LogisticRegression
model=LogisticRegression()
model.fit(x_sampled,y_sampled)
y_pred=model.predict(x_test)

model.coef_

model.intercept_

model.predict_proba(x_test)

"""##**Evaluate the model**"""

from sklearn.metrics import confusion_matrix,accuracy_score,precision_score,f1_score,recall_score

confusion_matrix(y_test,y_pred)

accuracy_score(y_test,y_pred)

precision_score(y_test,y_pred)

f1_score(y_test,y_pred)

recall_score(y_test,y_pred)

"""##**AUC ROC**"""

from sklearn.metrics import roc_curve,roc_auc_score

# prob
y_prob=model.predict_proba(x_test)[:,1]
y_prob

roc_auc_score(y_test,y_prob)

# fpr v/s tpr
fpr,tpr,threshold = roc_curve(y_test,y_prob)

# plot roc curve
plt.figure(figsize=(5,2))
plt.plot(fpr,tpr)
plt.plot([0,1],[0,1])
plt.xlabel('fpr')
plt.ylabel('tpr')
plt.title('roc curve')
plt.show()

"""##**KNN CLassifier**"""

from sklearn.neighbors import KNeighborsClassifier
model = KNeighborsClassifier(n_neighbors=7)
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

confusion_matrix(y_test,y_pred)

accuracy_score(y_test,y_pred)

recall_score(y_test,y_pred)

precision_score(y_test,y_pred)

f1_score(y_test,y_pred)

"""**How to choose best value for K**"""

error_rate=[]
for k in range(2,11):
  model = KNeighborsClassifier(n_neighbors=k)
  model.fit(x_train,y_train)
  y_pred = model.predict(x_test)
  error_rate.append(np.mean(y_test!=y_pred))
print(error_rate)

plt.figure(figsize=(5,2))
plt.plot(range(2,11),error_rate)
plt.xlabel('k')
plt.ylabel('error')
plt.show()

"""##**Decision tree classifier**"""

from sklearn.tree import DecisionTreeClassifier
model = DecisionTreeClassifier()
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

#evaluate
print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""#DT with Hyperparameters"""

from sklearn.tree import DecisionTreeClassifier
model = DecisionTreeClassifier(criterion='gini',max_depth=50,max_features='sqrt',splitter='best',min_samples_leaf=5,min_samples_split=6,random_state=42)
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

#evaluate
print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**Random forest classifier**"""

from sklearn.ensemble import RandomForestClassifier
model = RandomForestClassifier()
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

#evaluate
print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

from sklearn.ensemble import RandomForestClassifier
model=RandomForestClassifier(n_estimators=10,max_depth=50,bootstrap=True,criterion='gini',max_features='sqrt',min_samples_split=5,min_samples_leaf=6)
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

#evaluate
print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

import pickle
with open('model.pkl','wb') as f:
  pickle.dump(model,f)

"""##**Gradient Boosting**"""

from sklearn.ensemble import GradientBoostingClassifier
model=GradientBoostingClassifier()
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

from sklearn.ensemble import GradientBoostingClassifier
model=GradientBoostingClassifier(n_estimators=10,max_depth=50,learning_rate=0.01,
                                 max_features='sqrt',min_samples_split=5,
                                 min_samples_leaf=6)
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**XGBOOST Classifier**"""

from xgboost import XGBClassifier
model=XGBClassifier()
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**K Fold Cross validation**"""

from sklearn.model_selection import KFold,cross_val_score
model=XGBClassifier()
kf=KFold(n_splits=5,shuffle=True,random_state=42)
scores=cross_val_score(model,x_sampled,y_sampled,cv=kf)
print(scores)
print(np.mean(scores))

"""##**Stratified Cross validation**"""

from sklearn.model_selection import StratifiedKFold,cross_val_score
model=XGBClassifier()
kf=StratifiedKFold(n_splits=5,shuffle=True,random_state=42)
scores=cross_val_score(model,x_sampled,y_sampled,cv=kf)
print(scores)
print(np.mean(scores))

"""##**Hyper Parameters for XGBOOST**"""

from xgboost import XGBClassifier
model=XGBClassifier(n_estimators=10,max_depth=10,
                    learning_rate=0.1,gamma=0.1,subsample=0.8,
                    colsample_bytree=0.8,reg_alpha=0.1,reg_lambda=2,
                    n_jobs=1 )
model.fit(x_sampled,y_sampled)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**SVM**"""

from sklearn.svm import SVC
model = SVC()
model.fit(x_train,y_train)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

from sklearn.svm import SVC
model = SVC(kernel='rbf',gamma=0.01,C=20)
model.fit(x_train,y_train)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**grid Search CV**"""

from sklearn.model_selection import GridSearchCV
model=SVC()
parameters={'kernel':['linear','poly','rbf','sigmoid'],
            'gamma':[0.1,0.01,1.001,0.2,0.3,0.5],'C':[5,10,15,20,25,30]}
grid=GridSearchCV(estimator=model,param_grid=parameters,cv=5,scoring='accuracy')
grid.fit(x_sampled,y_sampled)

print(grid.best_params_)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**Randomised Search CV**"""

from sklearn.model_selection import RandomizedSearchCV
model=SVC()
parameters={'kernel':['linear','poly','rbf','sigmoid'],
            'gamma':[0.1,0.01,1.001,0.2,0.3,0.5],'C':[5,10,15,20,25,30]}
random=RandomizedSearchCV(estimator=model,cv=5,param_distributions=parameters,n_iter=20,scoring='accuracy')
random.fit(x_sampled,y_sampled)

print(random.best_params_)

from sklearn.svm import SVC
model = SVC(kernel='rbf',gamma=0.5,C=5)
model.fit(x_train,y_train)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

"""##**Grid search CV for XGBoost**"""

model=XGBClassifier()
parameters={'max_depth':[3,4,10,20,30,50],'n_estimators':[100,200,300,10,50,20],'learning_rate':[0.01,0.2,0.3,0.1],'reg_alpha':[0.1,0.01,0.01],'reg_lambda':[0.01,0.2,0.3,0.1]}
grid=GridSearchCV(estimator=model,param_grid=parameters,cv=5,scoring='accuracy')
grid.fit(x_sampled,y_sampled)

print(grid.best_params_)

from sklearn.svm import SVC
model = SVC(kernel='rbf',gamma=0.5,C=5)
model.fit(x_train,y_train)
y_pred = model.predict(x_test)

print("confusion matrix\n", confusion_matrix(y_test,y_pred))
print("accuracy score", accuracy_score(y_test,y_pred))
print("recall score", recall_score(y_test,y_pred))
print("precision score", precision_score(y_test,y_pred))
print("f1 score", f1_score(y_test,y_pred))

data